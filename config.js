// Shared GitHub Gist Configuration
// This file is no longer used for storing tokens (for security)
// Instead, use URL-based setup: ?token=YOUR_TOKEN&gist=YOUR_GIST_ID
// Or configure via the Settings page (admin only)

const SharedGistConfig = {
    // GitHub Personal Access Token (with 'gist' scope)
    // Create one at: https://github.com/settings/tokens
    // Leave empty - use URL parameters or Settings page instead
    token: '',
    
    // Gist ID - the shared Gist that stores all game data
    // Leave empty - use URL parameters or Settings page instead
    gistId: ''
};
